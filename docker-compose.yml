services:
  postgres:
    image: postgres:15
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: dayhoff
      POSTGRES_PASSWORD: dayhoff_dev
      POSTGRES_DB: dayhoff
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
